# Copy to .env for local (non-Docker) development.
# Docker Compose sets these directly in docker-compose.yml instead.

DATABASE_URL=postgresql+psycopg2://wa_mining:wa_mining@localhost:5432/wa_mining
CORS_ORIGINS=http://localhost:5173
